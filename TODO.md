When registering new shop - a color picker would be useful additionally to predetermined colour palette

In the list view when I search for items to add. The item name I search does not appear on the list - then hitting enter should open new item dialog. Saving that item should automatically add it to the list I was editing

additional logging is needed. I noticed multiple status 500 without any information on what had failed.

New item dialog should require a unit

editing list view should display shop dots on the right of the item, not below the name

editing list view should display tags on the right side of the item, not below the name. Tags should be above shop dots

editing list view should allow me to set required quantity of the item

