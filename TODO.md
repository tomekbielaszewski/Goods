Editing list view, on items already added to the list. Display unit as a text (not editable text field), and change default-looking numerical textfield with something which will let me edit that on my phone with my fingers. The UI right now is not phone friendly for this tiny component

Editing list view, on items already added to the list. Display the amount below the item name, as well as the unit

